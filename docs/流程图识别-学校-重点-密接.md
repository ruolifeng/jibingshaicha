# 结核病筛查流程图识别说明（Typora / Mermaid）

> **在 Typora 中查看**：请确保「偏好设置 → Markdown → Markdown 扩展语法」中已勾选 **「图表（Diagram）」**，或使用支持 Mermaid 的预览。下文流程图均使用 `mermaid` 代码块，可在 Typora 中直接渲染。

本文档按 **1·2·3（学校）**、**4·5·6·7（重点人群）**、**8·9·10·11（密接人群）** 组织；**图 2、5、9** 与 **图 6、10** 在三条主线间**逻辑共用**，仅入口不同。

---

## 一、学校人群（图 1、2、3）

### 图 1 — 筛查入口与分流

```mermaid
flowchart LR
  subgraph s1[" "]
    direction LR
    A["学校人群"] --> B{"学校筛查<br/>导入数据"}
    B -->|感染筛查阴性<br/>PPD/EC/IGRA 均阴| C["结束归档"]
    B -->|感染筛查阳性<br/>PPD+/++/+++ / EC阳 / IGRA阳| D["判断为疑似结核患者"]
    D --> E["追踪"]
    E --> F["系统中选择<br/>其他 / 未到位 / 到位"]
  end
```

### 图 2 — 追踪 → 胸片 → 转诊 → 诊断分流

（重点人群图 5、密接图 9 与此**同构**。）

```mermaid
flowchart LR
  subgraph track["追踪后"]
    direction LR
    S{"系统选择<br/>其他 / 未到位 / 到位"}
    S -->|其他| N1["备注原因"] --> E1["结束归档"]
    S -->|未到位| L["多次追踪<br/>直至到位"]
    L -->|未到位| L
    L -->|到位| AR["到位"]
    S -->|到位| AR
    AR --> X["上传胸片检查情况<br/>和诊断结果"]
    X --> RF["转诊"]
    RF --> DG{"根据诊断结果"}
  end

  DG -->|其它| N2["备注原因"] --> E2["结束归档"]
  DG -->|排除| E3["结束归档"]
  DG -->|疑似肺结核| SP["后续按疑似<br/>肺结核处理"]
  DG -->|潜伏感染者| LAT["进入潜伏感染者流程"]
  DG -->|确诊患者| PAT["患者管理"]
```

> 原图中「疑似肺结核」与转诊后其它分支的衔接以贵方定稿为准；上图为保持五分类完整而单列节点。

### 图 2（续）— 潜伏感染者 / 确诊患者 支线概要

```mermaid
flowchart LR
  subgraph latent["潜伏感染者支线"]
    direction LR
    LAT0["潜伏感染者"] --> M1["管理者→督导员<br/>发送通知"]
    M1 --> NTF["潜伏感染者通知单"]
    NTF -.->|48h未接受| R48a["生成消息提醒"]
    NTF --> PT["预防性治疗"]
    PT -.->|72h未完成督导<br/>及督导表| R72a["生成通知单提醒"]
  end

  subgraph patient["确诊患者支线"]
    direction LR
    PAT0["确诊患者"] --> PM["患者管理"]
    PM --> M2["管理者→督导员<br/>发送通知"]
    M2 --> PNT["患者通知单"]
    PNT -.->|48h未接受| R48b["生成消息提醒"]
  end
```

> 不同截图对「二级 / 三级管理者」表述不一致，图中统一写为「管理者」。

### 图 3 — 潜伏与确诊两条主流程（并行）

```mermaid
flowchart LR
  subgraph upper["上支：潜伏感染者"]
    direction LR
    U1["潜伏感染者通知单"]
    U1 -.->|48h未接受| U1r["生成消息提醒"]
    U1 --> U2["预防性治疗"]
    U2 -.->|72h未完成督导<br/>并填写督导表| U2r["生成通知单提醒"]
    U2 --> U3["填写预防性治疗<br/>督导表"]
    U3 --> U4["补充潜伏感染者<br/>管理情况表字段"]
    U4 --> U5["信息归集至<br/>潜伏感染者信息"]
  end

  subgraph lower["下支：确诊患者"]
    direction LR
    L0["管理者向督导员发送"] --> L1["患者通知单"]
    L1 -.->|48h未接受| L1r["生成消息提醒"]
    L1 --> L2["第一次入户随访记录表"]
    L2 -.->|72h未完成首次随访<br/>并填写随访表| L2r["生成消息提醒"]
    L2 --> L3["后续随访表"]
    L3 --> L4["治疗记录卡<br/>（可打印）"]
    L4 --> L5["信息归集至患者信息"]
  end
```

---

## 二、重点人群（图 4、5、6、7）

### 图 4 — 筛查入口与分流

```mermaid
flowchart LR
  SCR["筛查"] --> KP["重点人群"]
  KP --> DIA{"重点人群<br/>筛查导入数据"}
  DIA -->|感染筛查阴性| ARC["结束归档"]
  DIA -->|PPD+/++/+++ / EC阳 / IGRA阳| SUS["判断为疑似结核患者"]
  SUS --> TR["追踪"]
  TR --> F["接图5：系统选择<br/>其他/未到位/到位"]
```

### 图 5 — 与学校图 2 相同段落

见上文 **「图 2 — 追踪 → 胸片 → 转诊 → 诊断分流」**。

### 图 6 — 转诊后四种结局并列

```mermaid
flowchart LR
  subgraph p6["诊断后四路径（示意）"]
    direction LR
    OTH["其它"] --> R1["备注原因"] --> A1["结束归档"]
    EXC["排除"] --> A2["结束归档"]
    LAT6["潜伏感染者"] --> S1["管理者→督导员"] --> N1["潜伏感染者通知单"]
    N1 -.->|48h| RM1["消息提醒"]
    N1 --> PT6["预防性治疗"]
    PT6 -.->|72h| RM2["通知单提醒"]
    PT6 --> SF["填写督导表"] --> SUP["补充管理情况字段"] --> AGG1["归集潜伏信息"]

    CON["确诊患者"] --> MGT["患者管理"] --> S2["管理者→督导员"] --> N2["患者通知单"]
    N2 -.->|48h| RM3["消息提醒"]
    N2 --> FV["第一次入户随访"] -.->|72h| RM4["消息提醒"]
    FV --> NF["后续随访表"]
  end
```

> 部分原图将确诊支线写为「二级管理者」发送患者通知单，与上表「管理者」统一为可配置角色。

### 图 7 — 与图 3 同类（局部放大）

见上文 **「图 3 — 潜伏与确诊两条主流程」**。

---

## 三、密接人群（图 8、9、10、11）

### 图 8 — 三轮筛查与阴阳分流

（阳性三种情形：**首次阳** / **首阴半年后阳** / **首阴+半年阴+一年阳**；阴性情形：**首阴** / **首阴+半年阴** / **三轮均阴**。）

```mermaid
flowchart TB
  CC["密接人群"] --> IMP["密接人群导入表"]
  IMP --> BR{"感染筛查<br/>结果判定"}
  BR -->|任一轮为阳性类<br/>PPD+/++/+++ / EC阳 / IGRA阳| POSR["感染筛查阳性"]
  POSR --> SUS2["判断为疑似结核患者"]
  SUS2 --> TR2["追踪"]

  BR -->|三轮均为阴性类<br/>PPD/EC/IGRA 均阴| NEGR["感染筛查阴性"]
  NEGR --> ARC2["结束归档"]
```

### 图 9 — 追踪阶段（与学校图 2、重点图 5 相同）

见上文 **「图 2 — 追踪 → 胸片 → 转诊 → 诊断分流」**。

### 图 10 — 四种结局（与图 6 同构）

见上文 **「图 6 — 转诊后四种结局并列」**。

### 图 11 — 与图 3、7 同类

见上文 **「图 3 — 潜伏与确诊两条主流程」**。

---

## 四、总览：跨人群共性（一张总图）

```mermaid
flowchart TB
  subgraph screen["筛查阶段"]
    direction LR
    SCH["学校：筛查导入"] --> NEG1{全阴?}
    KEY["重点：筛查导入"] --> NEG2{全阴?}
    MC["密接：导入表<br/>三轮"] --> NEG3{三轮全阴?}
    NEG1 -->|是| AR1["归档"]
    NEG2 -->|是| AR1
    NEG3 -->|是| AR1
    NEG1 -->|否| SP1["疑似患者"]
    NEG2 -->|否| SP1
    NEG3 -->|否| SP1
  end

  SP1 --> TRK["追踪"]

  subgraph after["追踪后（三条主线共用）"]
    direction LR
    TRK --> CH{"其他/未到位/到位"}
    CH -->|其他| AR2["备注→归档"]
    CH -->|未到位| LOOP["多次追踪"] --> CH
    CH -->|到位| XR["胸片+诊断"] --> REF["转诊"] --> OUT{"诊断五分类"}
    OUT -->|其它/排除| AR3["归档"]
    OUT -->|潜伏| LFLOW["通知单→预防性治疗→督导表→归集"]
    OUT -->|确诊| PFLOW["患者管理→通知单→随访→治疗卡→归集"]
  end
```

---

## 五、截图文件与编号对应

| 约定编号 | 参考文件（工作区 assets） |
|----------|---------------------------|
| 1 | `CleanShot_2026-04-15_at_12.50.02-...png` |
| 2 | `CleanShot_2026-04-15_at_12.51.06-...png` |
| 3 | `CleanShot_2026-04-15_at_12.51.24-...png` |
| 4 | `CleanShot_2026-04-15_at_12.51.38-...png` |
| 5 | `CleanShot_2026-04-15_at_12.51.49-...png` |
| 6 | `CleanShot_2026-04-15_at_12.52.07-...png` |
| 7 | `CleanShot_2026-04-15_at_12.52.17-...png` |
| 8 | `CleanShot_2026-04-15_at_12.52.31-...png` |
| 9 | `CleanShot_2026-04-15_at_12.52.42-...png` |
| 10 | `CleanShot_2026-04-15_at_12.52.54-...png` |
| 11 | `CleanShot_2026-04-15_at_12.53.07-...png` |

---

## 六、Typora 说明与排错

| 项目 | 说明 |
|------|------|
| 语法 | 使用 **Mermaid**（`flowchart LR/TB` + `subgraph`） |
| 中文 | 节点文案放在 `["..."]` 或 `{"..."}` 内；换行用 `<br/>` |
| 虚线提醒 | `-.->` 表示超时提醒等旁路逻辑 |
| 不渲染 | 检查是否开启图表扩展；或升级 Typora 版本 |

*若贵方定稿流程与图中「疑似肺结核分支」「二级/三级管理者」不一致，只需改对应 Mermaid 块即可。*
