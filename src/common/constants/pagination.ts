/** 列表页每页条数可选项（全局统一，最大 1000 条，便于批量勾选导出/删除） */
export const PAGE_SIZE_OPTIONS = [10, 20, 50, 100, 500, 1000] as const

/** 默认每页条数 */
export const DEFAULT_PAGE_SIZE = 10

/** 单页最大条数 */
export const MAX_PAGE_SIZE = 1000
